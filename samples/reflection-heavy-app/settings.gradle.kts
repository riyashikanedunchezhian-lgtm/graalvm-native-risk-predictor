rootProject.name = "reflection-heavy-app"

// Pulls in the root project (native-risk-core, native-risk-gradle-plugin, etc.)
// as an included build, so this sample can apply the plugin directly from
// source without needing it published anywhere first.
includeBuild("../..")