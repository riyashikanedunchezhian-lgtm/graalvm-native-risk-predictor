# Sample: reflection-heavy-app

Deliberately exercises REFLECTION, DYNAMIC_PROXY, and
NON_CONSTANT_RESOURCE_LOAD findings -- used to verify those detectors
fire correctly end-to-end (unit tests cover detectors in isolation;
this exercises the full pipeline together).
